var app = {};

app.root_dir = __dirname;
app.config = require('../config')();
app.logger = require('../system/logger')


app.mGoose = require('../system/mGoose')(app);

console.log(app.config.db_host);